# Core numerics / analysis
numpy<2
pandas>=1.5,<3
matplotlib>=3.6,<4
torch>=2.0,<3

# Frontend
streamlit>=1.30,<2

# Stuff to make life easier
python-dateutil>=2.8
pytz>=2022.1
typing_extensions>=4.0
tqdm>=4.60
pytest>=7,<9

# NOTE:
# Graphviz PNG rendering needs the system "dot" binary.
# You need to install Graphviz separately (brew/apt), not via pip.
